/* Copy this file to config.js (gitignored) and paste your Supabase keys.
   On Vercel, set SUPABASE_URL and SUPABASE_ANON_KEY — the build script writes config.js. */
window.IBBN_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
};
